```ts
/**
 * prepend(value)
 * append(value)
 * insert(value, rawIndex)
 * delete(value)
 * find({ value, callback })
 * deleteTail()
 * deleteHead()
 * fromArray(values)
 * toArray()
 * toString()
 * reverse()
 */
```
